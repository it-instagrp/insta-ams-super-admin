/**
 * FILE: components/common/index.ts
 * Purpose: Shared UI/data logic for the Master Admin application.
 * NOTE: Keep presentation unchanged when refactoring; move repeated logic into reusable modules.
 */
/** Central export for reusable UI primitives. */
export { Button, ConfirmDialog, ModalShell, SearchInput, SelectField, StatCard } from "./ReusableComponents";
export type { ButtonVariant } from "./ReusableComponents";

/**
 * FILE: components/Users/ConfirmDialog.tsx
 * Purpose: Shared UI/data logic for the Master Admin application.
 * NOTE: Keep presentation unchanged when refactoring; move repeated logic into reusable modules.
 */
/** Compatibility export: the shared ConfirmDialog lives in components/common. */
export { ConfirmDialog as default } from "../common";
